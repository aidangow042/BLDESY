# Supabase

The website repo (`~/bldesy-web/supabase/`) owns the database schema, migrations and the deployed Edge Functions (`ai-chat`, `ai-job-suggest`). Nothing under this directory is a source of truth; the remaining function sources here are legacy callers that Phase 1 of the parity plan replaces with website API routes.
